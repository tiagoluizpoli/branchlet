{
  "$schema": "http://json-schema.org/draft-7/schema#",
  "description": "Configuration schema for Branchlet - Git worktree management CLI",
  "type": "object",
  "properties": {
    "worktreeCopyPatterns": {
      "description": "File patterns to copy to new worktrees (glob patterns supported)",
      "default": [
        ".env*",
        ".vscode/**"
      ],
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "worktreeCopyIgnores": {
      "description": "File patterns to ignore when copying (glob patterns supported)",
      "default": [
        "**/node_modules/**",
        "**/dist/**",
        "**/.git/**",
        "**/Thumbs.db",
        "**/.DS_Store"
      ],
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "worktreePathTemplate": {
      "description": "Base template for worktree directories. Relative paths use the repository parent; ~ and ~/... use the runtime home; native absolute paths retain their root (for example /tmp/worktrees or C:\\worktrees). Variables: $BASE_PATH, $WORKTREE_PATH, $BRANCH_NAME, $SOURCE_BRANCH",
      "default": "$BASE_PATH.worktree",
      "type": "string"
    },
    "postCreateCmd": {
      "description": "Commands to run after creating a worktree. Variables: $BASE_PATH, $WORKTREE_PATH, $BRANCH_NAME, $SOURCE_BRANCH",
      "default": [],
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "terminalCommand": {
      "description": "Command to open terminal in new worktree directory (e.g., 'code $WORKTREE_PATH')",
      "default": "",
      "type": "string"
    },
    "deleteBranchWithWorktree": {
      "description": "Also delete the associated git branch when deleting a worktree",
      "default": false,
      "type": "boolean"
    }
  },
  "$id": "https://raw.githubusercontent.com/raghavpillai/branchlet/main/schema.json",
  "title": "Branchlet Configuration",
  "additionalProperties": false
}
